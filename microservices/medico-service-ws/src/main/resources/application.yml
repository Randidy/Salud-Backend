server:
  port: 8092
spring:
  application:
    name: medico-service-ws
  config:
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
